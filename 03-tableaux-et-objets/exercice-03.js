// Exercice 03 : Utilisation de filter()
// Utilise filter() pour ne garder que les nombres supérieurs à 5.

let valeurs = [3, 6, 8, 2, 9];

let supérieursValue = valeurs.filter(n => n > 5);
console.log(supérieursValue);