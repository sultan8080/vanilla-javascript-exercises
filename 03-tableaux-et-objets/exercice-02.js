// Utilise map() pour doubler chaque nombre d’un tableau
let valeurs = [3, 6, 8, 2, 9];


let doubleValeurs = valeurs.map(n=> {
return n*2
})

// let doubleValeurs = valeurs.map(n=> n*2)

console.log(doubleValeurs);
